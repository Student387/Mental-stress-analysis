/**
 * Student Mental Stress Analysis System - Main JavaScript
 * Shared utilities and Chart.js helpers
 */

// Chart.js default config for consistency
const chartDefaults = {
    responsive: true,
    maintainAspectRatio: false,
    plugins: {
        legend: { position: 'bottom' }
    }
};
